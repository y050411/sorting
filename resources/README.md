# Resources

This directory contains application resources:

- `app_icon.ico` — Application icon (add your own .ico file here)

To add an application icon:
1. Place your icon file as `app_icon.ico` in this directory
2. The build script will automatically include it
